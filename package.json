{
  "name": "datalib",
  "version": "0.6.0",
  "description": "Test NPM Package",
  "files": [
    "/dist/**/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "postinstall": "ls dist > a.txt && echo < a.txt && cp -R dist/* ./"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MSex/datalib.git"
  },
  "author": "MSex",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/MSex/datalib/issues"
  },
  "homepage": "https://github.com/MSex/datalib#readme",
  "devDependencies": {
    "prettier": "^2.2.1",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "reflect-metadata": "^0.1.13",
    "rxjs": "^6.6.3"
  }
}
